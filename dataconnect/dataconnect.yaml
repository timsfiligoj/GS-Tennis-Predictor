specVersion: "v1"
serviceId: "gstennispredictor"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "gstennispredictor-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
